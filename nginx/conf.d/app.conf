server {
    listen 80 default_server;
    server_name kirillblog.ru;  # Замените на ваш домен
    
    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }
    
    location / {
        return 301 https://$server_name$request_uri;
    }
}